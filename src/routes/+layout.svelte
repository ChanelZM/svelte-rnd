<div class="wrapper">
  <slot />
</div>

<style lang="scss">
  .wrapper {
    max-width: 110rem;
    min-height: 100vh;
    margin: 0 auto;
    padding: 0 1rem;

    background-color: var(--tc-background-primary);
  }
</style>
