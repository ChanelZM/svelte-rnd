<script lang="ts">
</script>

<div class="card">
  <slot />
</div>

<style lang="scss">
  .card {
    padding: var(--spacing-large);
    background: var(--tc-background-primary);
    border: 1px solid var(--tc-decoration);
  }
</style>
